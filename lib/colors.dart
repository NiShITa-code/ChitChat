import 'dart:ui';

const backgroundColor =
    Color.fromRGBO(179, 195, 206, 1); // Very light gray background
const textColor = Color.fromRGBO(50, 50, 50, 1); // Dark gray text color
const appBarColor =
    Color.fromRGBO(204, 208, 209, 1); // Light grayish app bar color
const webAppBarColor =
    Color.fromRGBO(208, 212, 214, 1); // Slightly darker shade for web app bar
const messageColor = Color.fromRGBO(162, 200, 205, 1); // Soft blue for messages
const senderMessageColor =
    Color.fromRGBO(184, 184, 184, 1); // Light grayish sender message color
const tabColor = Color.fromRGBO(85, 207, 187, 1); // Teal tab color
const searchBarColor =
    Color.fromRGBO(188, 196, 203, 1); // Light gray search bar color
const dividerColor =
    Color.fromRGBO(201, 209, 212, 1); // Light gray divider color
const chatBarMessage =
    Color.fromRGBO(155, 173, 191, 1); // Light grayish chat bar color
const mobileChatBoxColor =
    Color.fromRGBO(160, 188, 216, 1); // Light gray chat box color
const greyColor = Color.fromRGBO(155, 173, 191, 1); // Light gray color
const blackColor = Color.fromRGBO(0, 0, 0, 1); // Black color
const daekBlueColor = Color.fromRGBO(1, 1, 42, 1); // Dark blue color